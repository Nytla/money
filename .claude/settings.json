{
  "permissions": {
    "allow": [
      "Bash(npm run lint)",
      "Bash(npm run typecheck)",
      "Bash(npm run format:check)",
      "Bash(npm test)",
      "Bash(npm run test:*)",
      "Bash(npm run build)",
      "Bash(npx prisma validate)",
      "Bash(npx prisma generate)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "statusMessage": "Prettier",
            "timeout": 60,
            "command": "try { $i = [Console]::In.ReadToEnd() | ConvertFrom-Json; $f = $i.tool_response.filePath; if (-not $f) { $f = $i.tool_input.file_path }; if ($f -match '\\.(ts|tsx|mts|mjs|json|md|css)$') { npx prettier --write --ignore-unknown $f | Out-Null } } catch {}"
          }
        ]
      }
    ]
  }
}
