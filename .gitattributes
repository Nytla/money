# Нормализуем переводы строк: в репозитории всегда LF, в рабочей копии — как есть.
# Без этого Windows-клиент постоянно показывает изменения во всех файлах.
* text=auto eol=lf

*.png binary
*.ico binary
*.xlsx binary
*.db binary
